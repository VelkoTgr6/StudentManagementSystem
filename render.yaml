services:
  - type: web
    name: student-management-system
    env: docker
    plan: free
    region: frankfurt
    dockerfilePath: ./Dockerfile
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: studentms-db
          property: connectionString
